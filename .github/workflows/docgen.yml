name: DocGen

on: 
  pull_request:
    paths:
      - "**/variables.tf"
      - "**/outputs.tf"

jobs:
  docgen:
    name: DocGen
    runs-on: ubuntu-latest

    steps:
    - name: Build new docs
      run: make docs
    - name: Push docs to PR
      run: make push-docs
